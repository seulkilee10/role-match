import React from 'react';
import ReactDOM from 'react-dom';
import RoleMatchingApp from './RoleMatchingApp';

ReactDOM.render(
  <React.StrictMode>
    <RoleMatchingApp />
  </React.StrictMode>,
  document.getElementById('root')
);
